using UnityEngine;

[CreateAssetMenu(fileName = "BubbleSettings", menuName = "Create Bubble Settings", order = 0)]
public class BubbleSettings : ScriptableObject
{
    public Color Color;
    public int Number;
}
